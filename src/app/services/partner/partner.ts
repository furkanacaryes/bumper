// eslint-disable-next-line
namespace Bumper {
	export type Partner = {
		name: string;
		company: string;
		mobile_phone: string;
		email_address: string;
		postcode: string;
		pay_later: boolean;
		pay_now: boolean;
	};
}
